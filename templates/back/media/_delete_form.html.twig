<form method="post" action="{{ path('admin_media_delete', {'id': media.id}) }}" onsubmit="return confirm('Etes-vous certain de vouloir supprimer ce média?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ media.id) }}">
	<button class="btn btn-purple my-2 col-4">Supprimer</button>
</form>
