{% extends 'back/base.html.twig' %}

{% block title %}
{{ media_category.name }}
{% endblock %}
{% block body %}
	<h1 class="text-center my-4" id="detail-title">{{ media_category.name }}</h1>

	<table class="table my-2 card-purple">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ media_category.id }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ media_category.name }}</td>
			</tr>
		</tbody>

	</table>

	<a href="{{ path('admin_media_category_edit', {id:media_category.id}) }}" class="btn btn-purple mx-2 my-2 col-4">Modifier</a>

	{{ include('back/media_category/_delete_form.html.twig') }}

	<a href="{{ path('admin_media_category_index') }}" class="btn btn-purple mx-2 my-2 col-4">Retour à la liste</a>

{% endblock %}
