{% extends 'back/base.html.twig' %}

{% block title %}
{{ type.name }}
{% endblock %}
{% block body %}
	<h1 class="text-center my-4" id="detail-title">{{ type.name }}</h1>

	<table class="table my-2 card-purple">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ type.id }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ type.name }}</td>
			</tr>
		</tbody>

	</table>

	<a href="{{ path('admin_type_edit', {id:type.id}) }}" class="btn btn-purple mx-2 my-2 col-4">Modifier</a>

	{{ include('back/type/_delete_form.html.twig') }}

	<a href="{{ path('admin_type_index') }}" class="btn btn-purple mx-2 my-2 col-4">Retour à la liste</a>

{% endblock %}