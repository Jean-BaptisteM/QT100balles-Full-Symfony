<form method="post" action="{{ path('admin_type_delete', {'id': type.id}) }}" onsubmit="return confirm('Etes-vous certain de vouloir supprimer ce genre ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type.id) }}">
    <button class="btn btn-purple my-2 col-4">Supprimer</button>
</form>
